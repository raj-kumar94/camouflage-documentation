<!--
  TUTORIAL TEMPLATE — copy the body below into a new page under popular-use-cases/ or
  configurations/. This file lives in the skill (not in SUMMARY.md) so it is NEVER published.
  Delete the HTML comments and fill in the CAPS placeholders.

  Structure (keep this order — the capture script and reader expectations both assume it):
    1. frontmatter description   → one sentence, shows on GitBook cards + search
    2. What & when               → problem statement + 2-4 real-world scenarios
    3. Hero visual               → a diagram (Track B) or annotated screenshot (Track A)
    4. Before you start          → prerequisites + links to Setup steps
    5. Steps                     → numbered; one screenshot per meaningful step, badge = step no.
    6. Worked example            → one concrete named case walked end to end
    7. Variations / patterns     → + {% content-ref %} cross-links
    8. Support escape hatch      → in-app chat / raj@codecrux.dev
-->
---
description: >-
  ONE SENTENCE saying what the reader achieves and when it's useful. This shows
  on the GitBook card and in search — make it a benefit, not a feature name.
---

# TITLE — plain, task-shaped ("Hide variants during a flash sale")

Open with ONE paragraph: the shopper problem this solves, in plain English. No internals.

Real-world scenarios:

* **SCENARIO NAME.** A concrete, named situation a merchant will recognise.
* **SCENARIO NAME.** Another. Aim for 2-4. Specifics ("Christmas Red", "Navy XL") beat abstractions.

<!-- HERO VISUAL — prefer a diagram when the concept is abstract, an annotated screenshot when it's a UI flow. -->
<figure><img src="../.gitbook/assets/ASSET.svg" alt="DESCRIBE the image for screen readers and for when it fails to load."><figcaption>One line: what the reader should take away from this image.</figcaption></figure>

## Before you start

1. Make sure the theme setup is done. Refer to [Step 1](../camouflage-setup-guide/basic-configuration.md).
2. ANY other prerequisite (correct plan, store timezone, a tag created, etc.).

## STEPS HEADING (e.g. "Hide a variant on a schedule")

1. First action. **Bold** the exact UI label the merchant clicks.
   <figure><img src="../.gitbook/assets/SHOT-step1.webp" alt="..."><figcaption></figcaption></figure>
2. Next action.
3. Final action → what they should see change.

## Worked example: NAMED CASE

Walk ONE realistic case end to end with real values, so the reader can pattern-match to their own store.

## Variations / common patterns

### PATTERN NAME

* How to adapt the steps for this case.

{% content-ref url="RELATED-PAGE.md" %}
[RELATED-PAGE.md](RELATED-PAGE.md)
{% endcontent-ref %}

## Still stuck?

Open the in-app chat from your Camouflage dashboard, or email [raj@codecrux.dev](mailto:raj@codecrux.dev) and our team will set it up for you.
